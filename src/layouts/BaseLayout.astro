---
import BaseHead from "@components/BaseHead.astro";
import { SITE_TITLE, SITE_DESCRIPTION, AUTHOR } from "@consts";
import Link from "@components/Link.astro";
import "../styles/global.css";
import socialShareImage from "../images/social-share.png";

interface Props {
  title?: string;
  description?: string;
  type?: "website" | "article";
  publishedTime?: string;
  modifiedTime?: string;
  author?: string;
}

const {
  title = SITE_TITLE,
  description = SITE_DESCRIPTION,
  type = "website",
  publishedTime,
  modifiedTime,
  author,
} = Astro.props;

const today = new Date();

const posthogKey = import.meta.env.PUBLIC_POSTHOG_KEY;
const posthogHost =
  import.meta.env.PUBLIC_POSTHOG_HOST || "https://us.i.posthog.com";
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead
      title={title}
      description={description}
      image={socialShareImage}
      type={type}
      publishedTime={publishedTime}
      modifiedTime={modifiedTime}
      author={author}
    />
    <script is:inline define:vars={{ posthogKey, posthogHost }}>
      !(function (t, e) {
        var o, n, p, r;
        e.__SV ||
          ((window.posthog = e),
          (e._i = []),
          (e.init = function (i, s, a) {
            function g(t, e) {
              var o = e.split(".");
              (2 == o.length && ((t = t[o[0]]), (e = o[1])),
                (t[e] = function () {
                  t.push([e].concat(Array.prototype.slice.call(arguments, 0)));
                }));
            }
            (((p = t.createElement("script")).type = "text/javascript"),
              (p.crossOrigin = "anonymous"),
              (p.async = !0),
              (p.src =
                s.api_host.replace(".i.posthog.com", "-assets.i.posthog.com") +
                "/static/array.js"),
              (r = t.getElementsByTagName("script")[0]).parentNode.insertBefore(
                p,
                r,
              ));
            var u = e;
            for (
              void 0 !== a ? (u = e[a] = []) : (a = "posthog"),
                u.people = u.people || [],
                u.toString = function (t) {
                  var e = "posthog";
                  return (
                    "posthog" !== a && (e += "." + a),
                    t || (e += " (stub)"),
                    e
                  );
                },
                u.people.toString = function () {
                  return u.toString(1) + ".people (stub)";
                },
                o =
                  "init capture register register_once register_for_session unregister unregister_for_session getFeatureFlag getFeatureFlagPayload isFeatureEnabled reloadFeatureFlags updateEarlyAccessFeatureEnrollment getEarlyAccessFeatures on onFeatureFlags onSessionId getSurveys getActiveMatchingSurveys renderSurvey canRenderSurvey identify setPersonProperties group resetGroups setPersonPropertiesForFlags resetPersonPropertiesForFlags setGroupPropertiesForFlags resetGroupPropertiesForFlags reset get_distinct_id getGroups get_session_id get_session_replay_url alias set_config startSessionRecording stopSessionRecording sessionRecordingStarted captureException loadToolbar get_property getSessionProperty createPersonProfile opt_in_capturing opt_out_capturing has_opted_in_capturing has_opted_out_capturing clear_opt_in_out_capturing debug".split(
                    " ",
                  ),
                n = 0;
              n < o.length;
              n++
            )
              g(u, o[n]);
            e._i.push([i, s, a]);
          }),
          (e.__SV = 1));
      })(document, window.posthog || []);
      if (posthogKey) {
        window.posthog.init(posthogKey, {
          api_host: posthogHost,
          person_profiles: "identified_only",
        });
      }
    </script>
  </head>
  <body
    class="my-12 mx-auto px-6 max-w-3xl bg-zinc-900 text-zinc-300 text-base leading-6 font-light font-mono"
  >
    <!-- Skip to main content link for screen readers -->
    <a
      href="#main-content"
      class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 bg-zinc-800 text-zinc-50 px-4 py-2 rounded z-50"
    >
      Skip to main content
    </a>

    <slot name="header" />

    <main id="main-content" tabindex="-1">
      <slot />
    </main>

    <footer
      class="my-12 text-zinc-400"
      role="contentinfo"
      aria-label="Site footer"
    >
      <nav aria-label="Social links">
        <Link
          href={AUTHOR.linkedin}
          external
          variant="subtle"
          class="hover:text-zinc-50 transition-colors group"
          aria-label={`Visit ${AUTHOR.name}'s LinkedIn profile`}
        >
          <span class="group-hover:text-zinc-400 transition-colors">[</span
          >LinkedIn<span class="group-hover:text-zinc-400 transition-colors"
            >]</span
          >
        </Link>
      </nav>
      <p class="mt-2">
        &copy; {today.getFullYear()}
        {AUTHOR.name}. All rights reserved.
      </p>
    </footer>
  </body>
</html>

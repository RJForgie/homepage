---
import BaseLayout from "./BaseLayout.astro";
import type { CollectionEntry } from "astro:content";
import ArticleMeta from "@components/ArticleMeta.astro";
import BackLink from "@components/BackLink.astro";

type Props = CollectionEntry<"posts" | "lists">["data"];

const { title, description, pubDate, updatedDate } = Astro.props;

// Format dates for meta tags
const publishedTime = pubDate.toISOString();
const modifiedTime = updatedDate ? updatedDate.toISOString() : publishedTime;
---

<BaseLayout
  title={title}
  description={description}
  type="article"
  publishedTime={publishedTime}
  modifiedTime={modifiedTime}
>
  <article itemscope itemtype="https://schema.org/Article" class="mt-12">
    <header>
      <h1 class="text-zinc-50 text-base font-medium mb-2" itemprop="headline">
        {title}
      </h1>
      <ArticleMeta pubDate={pubDate} updatedDate={updatedDate} />
    </header>
    <div
      class="prose-invert prose-base prose-h2:text-base prose-h2:font-medium prose-a:underline prose-ol:list-decimal prose-ul:list-disc prose-blockquote:border-l-2 prose-blockquote:border-zinc-600 prose-blockquote:pl-4 mt-12"
      itemprop="articleBody"
    >
      <slot />
    </div>
  </article>
  <nav class="mt-12" aria-label="Article navigation">
    <BackLink />
  </nav>
</BaseLayout>
